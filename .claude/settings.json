{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); if [[ \"$file\" =~ /supabase/(schemas|migrations)/.*\\.sql$ ]]; then (cd \"${CLAUDE_PROJECT_DIR:-$PWD}/client\" && pnpm db:gen-types >/dev/null 2>&1) || true; fi",
            "statusMessage": "Regenerating database types..."
          }
        ]
      }
    ]
  }
}
